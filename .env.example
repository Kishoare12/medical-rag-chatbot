# Backend API URL (for Streamlit frontend)
# For local development:
# API_URL=http://127.0.0.1:8000/query

# For production/deployment:
# API_URL=https://your-backend-domain.com/query

# Data directory path (for indexing)
# DATA_DIR=/path/to/your/medical/documents

# Chroma DB directory (where indexed documents are stored)
# CHROMA_DB_DIR=./chroma_db
